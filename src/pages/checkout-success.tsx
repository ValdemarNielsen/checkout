// Description: Checkout success page, for when a user has successfully completed a purchase and is redirected to this page showing "succesful" purchase.
import React from "react"

export default function CheckoutSuccess() {
    return (
        <div>
            <h1>Thank you for your purchase!</h1>
        </div>
    )

    //Showing list of items purchased from store (price, quantity, etc.)
}